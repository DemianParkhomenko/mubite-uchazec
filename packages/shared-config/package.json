{
  "name": "@mubite/shared-config",
  "version": "1.0.0",
  "description": "Shared configuration utilities for MUBITE monorepo",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mubite/shared-types": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
